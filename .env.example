# Copy this file to .env and fill in your key.
# The key is read by the Vite dev server only (vite.config.ts) and injected
# into proxied requests server-side. It is never bundled into browser code.
ANTHROPIC_API_KEY=sk-ant-...
